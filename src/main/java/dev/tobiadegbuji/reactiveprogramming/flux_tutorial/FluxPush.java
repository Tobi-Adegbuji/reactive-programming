package dev.tobiadegbuji.reactiveprogramming.flux_tutorial;

public class FluxPush {

    //Flux.push can also replace Flux.create
    //The only difference is that Flux.push is NOT thread safe
    //while Flux.create is thread safe.

    //If you will only have a single
}
